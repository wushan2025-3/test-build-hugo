{
  "name": "test-hugo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npx hugo",
    "serve": "npx hugo server"
  },
  "devDependencies": {
    "hugo-extended": "^0.166.0"
  }
}
